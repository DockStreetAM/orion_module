# Example environment variables for trade instance demo
#
# Option 1: Copy to .env in this directory
# Option 2: Use the project root .env file (script will find it)
#
# The script looks for .env in examples/ first, then falls back to project root.

ECLIPSE_USER=your_username
ECLIPSE_PWD=your_password
